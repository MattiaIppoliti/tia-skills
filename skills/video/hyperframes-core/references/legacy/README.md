# Legacy HTML-contract references

These files document the retired HTML + `data-*` + paused-GSAP-timeline contract. They are kept for one purpose: working on a project that is still on it.

**Do not follow these when building anything new.** The current contract is Remotion; it lives in this skill's `SKILL.md`. Start from `../legacy-html-contract.md`, which explains how to recognise a legacy project, whether to convert it, and the mapping across.
